{
  "manifest_version": 2,
  "name": "Lock Overlay",
  "version": "1.0",
  "description": "Prevents scrolling to the top of the page",
  "permissions": ["tabs"],
  "background": {
    "scripts": ["background.js"],
    "persistent": false
  }
}
